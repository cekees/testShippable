cache: true

language: python

env:
- TEST_PROFILE="test.proteus.yaml"

python:
- 2.7

before_install:
- pwd
#- sudo apt-get install gfortran cmake libblas-dev liblapack-dev
#- wget http://proteus.usace.army.mil/files/hashdist.tgz
#- tar xzf hashdist.tgz 
#- mv .hashdist $HOME

install:
- make hashdist
- make stack
- make profile

script:
- ./linux2/bin/python -c "import  numpy;  numpy.test()"

notifications:

email: false
