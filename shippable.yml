cache: true

language: python

env:
- TEST_PROFILE="test.proteus.yaml"

python:
- 2.7

before_install:
- pwd
- sudo apt-get install gfortran
- make hashdist
- make stack

install:
- make profile

script:
- make profile

notifications:

email: false
